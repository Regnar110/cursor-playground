{
  "name": "tme-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "nx dev tmeNext",
    "build": "nx build tmeNext",
    "lint": "nx lint tmeNext",
    "compose:up": "docker compose up -d --build",
    "compose:down": "docker compose down",
    "k6:load": "nx run tmeNext-K6Test:load",
    "k6:single-flight": "nx run tmeNext-K6Test:single-flight",
    "k6:invalidation": "nx run tmeNext-K6Test:invalidation"
  },
  "devDependencies": {
    "nx": "^22.7.5"
  }
}
